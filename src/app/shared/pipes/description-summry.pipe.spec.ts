import { DescriptionSummryPipe } from './description-summry.pipe';

describe('DescriptionSummryPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionSummryPipe();
    expect(pipe).toBeTruthy();
  });
});
